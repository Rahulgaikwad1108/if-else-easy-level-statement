#include <stdio.h>

int main() {
    int num = 30;
    if (num % 5 == 0 && num % 3 == 0) {
        printf("%d is divisible by 5 and 3.\n", num);
    } else {
        printf("%d does not satisfy the condition.\n", num);
    }
    return 0;
}
