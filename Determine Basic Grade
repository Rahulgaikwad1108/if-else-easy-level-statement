#include <stdio.h>

int main() {
    int marks = 75;
    if (marks >= 80) {
        printf("Grade A\n");
    } else if (marks >= 60) {
        printf("Grade B\n");
    } else {
        printf("Grade C or F\n");
    }
    return 0;
}
