#include <stdio.h>

int main() {
    int val = -9;
    if (val > 0) {
        printf("Positive.\n");
    } else if (val < 0) {
        printf("Negative.\n");
    } else {
        printf("Zero.\n");
    }
    return 0;
}
