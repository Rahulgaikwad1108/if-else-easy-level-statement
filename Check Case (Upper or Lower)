#include <stdio.h>

int main() {
    char c = 'Q';
    if (c >= 'a' && c <= 'z') {
        printf("%c is lowercase.\n", c);
    } else if (c >= 'A' && c <= 'Z') {
        printf("%c is uppercase.\n", c);
    } else {
        printf("Not a standard letter.\n");
    }
    return 0;
}
