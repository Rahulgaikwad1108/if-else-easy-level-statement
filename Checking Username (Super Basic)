#include <stdio.h>
#include <string.h>

int main() {
    char user[10] = "user123";
    if (strcmp(user, "admin") == 0) {
        printf("Welcome, Admin.\n");
    } else {
        printf("Welcome, Guest.\n");
    }
    return 0;
}
